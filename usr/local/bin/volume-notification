#!/bin/bash

volume=$(awk -F"[][]" '/Left:/ { print $2 }' <(amixer sget Master) | sed 's/.$//')
muted=$(awk -F"[][]" '/Left:/ { print $4 }' <(amixer sget Master))
title="Volume"

if [ "$muted" = "off" ]; then
	title="Volume (MUTED)"
fi

dunstify -h int:value:"$volume" -t 1000 "$title"
